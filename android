# Andas SDK 更新日志

## Version 0.0.1.rc3 (2025-12-19)

### 🚀 重大变更

#### DataFrame API 统一化
- **修改 `getRow` 方法返回类型**：从 `Map<String, Any?>` 改为 `Map<String, Series<Any>?>`
- **统一所有行访问方法**：`loc()`, `iloc()`, `operator get()` 现在都返回 `Map<String, Series<Any>?>`
- **更新 `filter` 方法**：谓词函数参数类型改为 `Map<String, Series<Any>?>`

### ✨ 新增功能

#### Series 增强
- 每个单元格现在封装为单元素 `Series` 对象
- 支持在行级别使用 Series 的所有功能
- 更好的空值处理和类型安全

#### API 一致性
```kotlin
// 旧版本
val row: Map<String, Any?> = df.getRow(0)
val age = row["age"]  // Any?

// 新版本 (0.0.1.rc3)
val row: Map<String, Series<Any>?> = df.getRow(0)
val age = row["age"]?.get(0)  // Any?，但通过 Series 访问
```

### 🔧 优化改进

#### 性能优化
- 行访问操作现在返回轻量级 Series 对象
- 为未来向量化操作打下基础
- 减少类型转换开销

#### API 设计
- 统一的接口设计，减少认知负担
- 更好的支持链式操作
- 为未来功能扩展预留空间

### 🐛 修复

#### 类型安全
- 修复了 `filter` 方法中的类型推断问题
- 确保 `GroupBy` 操作与新的行访问 API 兼容

#### 空值处理
- 改进空值在行访问中的处理
- 确保 `null` 值正确转换为 `null` Series

### 📝 代码示例

#### 基本使用
```kotlin
val df = DataFrame(mapOf(
    "name" to listOf("Alice", "Bob", "Charlie"),
    "age" to listOf(25, 30, 35),
    "salary" to listOf(50000, 60000, 70000)
))

// 获取行
val row = df.getRow(0)
val name = row["name"]?.get(0)  // "Alice"
val age = row["age"]?.get(0)    // 25

// 使用 loc/iloc
val row1 = df.loc(0)           // Map<String, Series<Any>?>
val row2 = df.iloc(1)          // Map<String, Series<Any>?>
val row3 = df[2]               // Map<String, Series<Any>?>
```

#### 筛选操作
```kotlin
// 使用新的 Series-based API
val filtered = df.filter { row ->
    val age = row["age"]?.get(0) as? Int
    val salary = row["salary"]?.get(0) as? Int
    age != null && salary != null && age > 27 && salary > 55000
}
```

#### 空值处理
```kotlin
val dfWithNulls = DataFrame(mapOf(
    "name" to listOf("Alice", null, "Charlie"),
    "age" to listOf(25, 30, null)
))

val row = dfWithNulls.getRow(1)
val name = row["name"]?.get(0)  // null
val age = row["age"]?.get(0)    // 30
```

### ⚠️ 破坏性变更

#### API 变更
- `getRow()`, `loc()`, `iloc()`, `filter()` 的返回类型/参数类型已变更
- 现有代码需要相应调整以使用新的 Series-based API

#### 迁移指南
1. 将 `row["column"]` 改为 `row["column"]?.get(0)`
2. 在 `filter` 谓词中，使用 `row["column"]?.get(0)` 访问值
3. 如需要原始值，可使用 `series?.get(0)` 提取

### 🔮 未来展望

#### 即将到来的功能
- 行级 Series 操作的更多优化
- 支持更复杂的行级计算
- 增强的错误处理和调试支持

### 📋 其他说明

- 版本号更新：0.0.1.rc1 → 0.0.1.rc3
- 构建配置优化
- 文档更新

---

## 之前的版本

### Version 0.0.1.rc1 (2025-12-19)

#### 初始发布候选版本
- 基础 DataFrame 和 Series 实现
- JNI 原生高性能操作
- 完整的 IO 操作支持
- 分组聚合功能
- 发布配置和脚本

### 技术特性
- ✅ 高性能原生计算 (C++/OpenMP)
- ✅ 类 Pandas API 设计
- ✅ 异步操作支持
- ✅ Android 平台优化
- ✅ Maven 发布配置
